parent=keycloak.v2
import=common/keycloak

styles=css/styles.css

# The app itself has no dark palette yet (see frontend/src/app/globals.css) — force
# the login page to stay on the light Planazo palette regardless of OS preference,
# instead of inheriting keycloak.v2's automatic light/dark switch.
darkMode=false
