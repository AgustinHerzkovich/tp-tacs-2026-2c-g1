import { LoadingState } from "@/components/common/AsyncState";

export default function Loading() {
  return <main className="min-h-screen flex items-center justify-center"><LoadingState /></main>;
}
